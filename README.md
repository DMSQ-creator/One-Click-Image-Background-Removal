# 💎 AI 旗舰级抠图工具 Pro (InSPyReNet版)

这是一个基于 **Streamlit** 和 **InSPyReNet** 构建的本地化抠图应用。它不同于普通的快速抠图工具，专注于**发丝级、半透明物体（婚纱、玻璃）**的极致抠图效果。

![Status](https://img.shields.io/badge/Status-Stable-green) ![Python](https://img.shields.io/badge/Python-3.8%2B-blue) ![GPU](https://img.shields.io/badge/GPU-Recommended-orange)

## ✨ 核心功能

*   **旗舰画质**：使用业界顶尖的 InSPyReNet 模型，完美处理复杂边缘。
*   **批量处理**：支持一次性上传多张图片，自动排队处理。
*   **一键打包**：处理完成后，自动生成 ZIP 压缩包，一键下载所有结果。
*   **智能预览**：自动生成灰白棋盘格背景，直观查看半透明区域的抠图质量。
*   **隐私安全**：完全本地运行，图片无需上传至第三方服务器。

---

## 🛠️ 安装指南

### 1. 环境准备
确保你的电脑已安装 [Python 3.8 或更高版本](https://www.python.org/downloads/)。

### 2. 安装依赖
打开终端（CMD / PowerShell / Terminal），运行以下命令安装基础依赖：

```bash
pip install streamlit transparent-background pillow
```

### 3. (可选) 启用 GPU 加速 🚀
*强烈建议有 NVIDIA 显卡的用户安装 GPU 版 PyTorch，速度可提升 10-50 倍。*

请前往 [PyTorch 官网](https://pytorch.org/get-started/locally/) 复制对应的安装命令。通常如下（以 CUDA 11.8 为例）：

```bash
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```
*(如果只使用 CPU，直接运行 `pip install torch torchvision` 即可，但处理速度会较慢)*

---

## 🚀 启动与使用

### 1. 启动应用
在包含代码文件 (`app_ultra_pro.py`) 的目录下，运行：

```bash
streamlit run app_ultra_pro.py
```

稍等片刻，浏览器会自动打开 `http://localhost:8501`。

### 2. 使用步骤

1.  **左侧设置**：
    *   **精度等级**：
        *   `旗舰画质 (Base)`：效果最好，细节最丰富（推荐）。
        *   `快速画质 (Fast)`：速度稍快，细节略有损失。
    *   **分辨率限制**：
        *   推荐选择 `2048` 或 `4096`。如果不限制，处理超大图片可能导致内存溢出。
2.  **上传图片**：
    *   支持 JPG, PNG, WEBP 格式。支持按住 `Ctrl` 多选上传。
3.  **开始处理**：
    *   点击 **▶️ 立即开始抠图**。
    *   *注意：首次运行时，程序会自动下载模型权重（约 150MB+），请保持网络畅通。*
4.  **查看与下载**：
    *   右侧主界面会实时显示处理进度和对比图（原图 vs 棋盘格预览）。
    *   点击顶部的 **📦 一键打包下载所有 (ZIP)** 获取全部文件。

---

## ❓ 常见问题 (Q&A)

**Q: 第一次点击开始后卡住不动了？**
> **A:** 首次运行需要下载 InSPyReNet 模型权重文件。请关注终端（黑框框）里的输出，可能会显示下载进度。如果网络不通畅，请尝试开启代理。

**Q: 报错 `Out of memory` 或程序崩溃？**
> **A:** 可能是图片分辨率过大。请在左侧侧边栏将“图片最大边长限制”设置为 `2048`。如果是在 CPU 模式下运行，内存不足也会导致此问题。

**Q: Linux 服务器上报错 `libGL.so not found`？**
> **A:** 这是 OpenCV 的依赖问题。请安装无头版 OpenCV：
> `pip install opencv-python-headless`
> 或者系统级安装：`sudo apt-get install libgl1-mesa-glx`

**Q: 处理一张图需要多久？**
> **A:**
> *   **NVIDIA GPU (如 RTX 3060)**: 约 0.2 ~ 0.5 秒/张。
> *   **普通 CPU**: 约 3 ~ 10 秒/张（取决于图片大小）。

---

## 📂 文件结构

```text
Project/
├── app_ultra_pro.py     # 主程序代码
├── README.md            # 本说明文档
```

---

*Powered by [InSPyReNet](https://github.com/plemeri/InSPyReNet) & [Transparent-Background](https://github.com/plemeri/transparent-background)*
